name: Keep Alive

on:
  schedule:
    - cron: '0 9 * * 1'
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Keep alive ping
        run: echo "Keeping workflow active - $(date)"
